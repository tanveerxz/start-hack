version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip3 install -r requirements.txt

run:
  runtime-version: 3.11
  pre-run:
    - pip3 install -r requirements.txt
  command: python3 -m uvicorn main:app --host 0.0.0.0 --port 8080
  secrets:
    - name: ANTHROPIC_API_KEY
      value-from: arn:aws:secretsmanager:us-west-2:348590979395:secret:Anthropic-6WNooP